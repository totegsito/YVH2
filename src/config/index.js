"use strict";
exports.__esModule = true;
var config = {
    port: process.env.PORT || 8888,
    host: process.env.HOST || 'localhost',
    maxDistance: process.env.MAX_DISTANCE || 100
};
exports["default"] = config;
